#include <iostream>

using namespace std;

bool check (int num, int nn) {
	if (num%nn==0 || num%10==nn || num/10==nn || num/100==nn) {
		return 1;
	}
	return 0;
}

int main(){
	int n;
	cin >> n;
	
	for (int i=1; i<=100; i++) {
		if (check(i, n)) {
			cout << "Clap";
		} else {
			cout << i;
		}
		cout << " ";
		if (i%10 == 0) {
			cout << endl;
		}
	}
}
